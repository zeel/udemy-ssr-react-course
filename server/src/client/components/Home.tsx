import React from 'react';

const Home = () => {
  return (
    <div>
      <div>I am new Home Component</div>
      <button onClick={() => console.log('clicked!')}>Press me!</button>
    </div>
  );
};

export default Home;
